spring.application.name=railway-os
# Server Port
server.port=8080

# MySQL Database Connection
spring.datasource.url=jdbc:mysql://localhost:3306/railway_db?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Sanket@2809
# (Leave password blank if using default XAMPP, otherwise enter your MySQL password)

# Hibernate Configuration (Auto-creates your tables!)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect